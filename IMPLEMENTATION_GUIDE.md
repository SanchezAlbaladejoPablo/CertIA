# Guía de Implementación - CertIA Wizard Mejorado\n\n## Estado Actual\n\nSe ha completado la implementación de funcionalidades críticas del CertificateWizard según el PRD:\n\n### ✅ Completado\n\n1. **Servicios de Cálculos Eléctricos** (`server/services/electrical-calculations.ts`)\n   - Cálculo de secciones de cable según REBT (ITC-BT-07)\n   - Cálculo de caída de tensión\n   - Cálculo de intensidad de cortocircuito\n   - Cálculo de protecciones (IGA y RCD)\n   - Sugerencias de circuitos típicos por tipo de instalación\n   - Validaciones según normas españolas\n\n2. **Servicios de Generación de Diagramas** (`server/services/diagram-generation.ts`)\n   - Generación de código Mermaid para esquemas unifilares\n   - Generación de representación textual\n   - Generación de HTML para visualización\n   - Soporte para circuitos con protecciones\n\n3. **Servicios de Generación de PDF** (`server/services/pdf-generation.ts`)\n   - Generación de HTML para certificados\n   - Template profesional según REBT\n   - Incluye todos los datos técnicos\n   - Listo para convertir a PDF con herramientas externas\n\n4. **Routers tRPC** (`server/routers.ts`)\n   - `calculations.cableSection`: Calcula sección de cable\n   - `calculations.mainSwitch`: Calcula IGA\n   - `calculations.earthResistance`: Calcula resistencia de tierra\n   - `ai.suggestCircuits`: Sugiere circuitos típicos\n   - `ai.generateDiagram`: Genera esquema unifilar\n   - `pdf.generateCertificate`: Genera certificado HTML\n\n5. **Componentes Frontend Mejorados**\n   - `CertificateWizardStep4Enhanced.tsx`: Step 4 con IA y cálculos\n   - `CertificateWizardStep6Enhanced.tsx`: Step 6 con diagrama y PDF\n\n## Próximos Pasos de Integración\n\n### 1. Actualizar CertificateWizard.tsx\n\nReemplaza el import y uso de Step 4 y Step 6:\n\n```typescript\n// Cambiar de:\nimport Step4Circuits from './Step4Circuits';\nimport Step6Review from './Step6Review';\n\n// A:\nimport Step4Circuits from './CertificateWizardStep4Enhanced';\nimport Step6Review from './CertificateWizardStep6Enhanced';\n\n// Y en el JSX:\n{currentStep === 4 && <Step4Circuits formData={formData} setFormData={setFormData} />}\n{currentStep === 6 && <Step6Review formData={formData} clients={clients} installations={installations} />}\n```\n\n### 2. Instalar Dependencias Adicionales (Opcional)\n\nPara conversión de HTML a PDF en servidor:\n\n```bash\npnpm add html2pdf puppeteer\n# O usar wkhtmltopdf (CLI)\n```\n\n### 3. Mejorar Step 2 - Instalación\n\nAsegurar que se guarde correctamente `installationType`:\n\n```typescript\nif (currentStep === 2 && formData.newInstallation && formData.clientId) {\n  try {\n    const result = await createInstallationMutation.mutateAsync({\n      clientId: formData.clientId,\n      type: formData.installationType,  // ← Agregar esto\n      ...formData.newInstallation,\n    });\n    // ...\n  } catch (error) {\n    // ...\n  }\n}\n```\n\n### 4. Agregar Persistencia de Borradores\n\nGuardar automáticamente cada N segundos:\n\n```typescript\nuseEffect(() => {\n  const timer = setInterval(() => {\n    if (formData.clientId || formData.installationId) {\n      // Guardar como borrador\n      saveDraftMutation.mutate(formData);\n    }\n  }, 30000); // Cada 30 segundos\n\n  return () => clearInterval(timer);\n}, [formData]);\n```\n\n### 5. Mejorar Step 3 - Derivación\n\nAgregar cálculos automáticos:\n\n```typescript\n// Cuando cambia diLength o diCableSection\nconst calculateDISection = async () => {\n  const result = await trpc.calculations.cableSection.useQuery({\n    power: formData.installedPower,\n    voltage: formData.supplyVoltage,\n    phases: formData.phases,\n    length: parseFloat(formData.diLength),\n    material: formData.diCableMaterial,\n  });\n  // Actualizar formData con resultado\n};\n```\n\n## Funcionalidades por Step\n\n### Step 1: Cliente ✅\n- Seleccionar cliente existente o crear nuevo\n- Validación de campos requeridos\n\n### Step 2: Instalación ⚠️\n- Seleccionar instalación existente o crear nueva\n- **TODO**: Guardar tipo de instalación correctamente\n- **TODO**: Agregar búsqueda de instalaciones por cliente\n\n### Step 3: Derivación ⚠️\n- Datos de derivación individual\n- Datos del cuadro general\n- **TODO**: Agregar cálculos automáticos de sección DI\n- **TODO**: Agregar sugerencias de IGA según potencia\n\n### Step 4: Circuitos ✅\n- ✅ Sugerir circuitos típicos con IA\n- ✅ Cálculo automático de sección de cable\n- ✅ Agregar/editar/eliminar circuitos\n- ✅ Validaciones\n\n### Step 5: Mediciones ✅\n- Resistencia de aislamiento\n- Resistencia de continuidad\n- Test RCD\n- Observaciones\n\n### Step 6: Revisión ✅\n- ✅ Resumen de todos los datos\n- ✅ Generación de esquema unifilar (Mermaid)\n- ✅ Generación de certificado (HTML)\n- ✅ Descarga de PDF\n- ✅ Lista de verificación\n\n## Arquitectura de Cálculos\n\n### Fórmulas Implementadas\n\n**Intensidad:**\n- Monofásico: I = P / V\n- Trifásico: I = P / (√3 * V)\n\n**Sección de Cable (Caída de Tensión):**\n- Monofásico: S = 2 * I * L * ρ / e\n- Trifásico: S = √3 * I * L * ρ / e\n\n**Resistencia de Tierra:**\n- R = U / I (donde U = 50V, I = corriente diferencial)\n\n### Tablas de Referencia\n\n- ITC-BT-19: Intensidad máxima admisible por sección\n- ITC-BT-07: Caída de tensión máxima permitida\n- ITC-BT-18: Puesta a tierra\n- ITC-BT-25: Circuitos típicos por tipo de instalación\n\n## Flujo de Generación de Certificado\n\n```\nUsuario completa wizard\n         ↓\n[Click \"Generar Esquema\"]\n         ↓\ntrpc.ai.generateDiagram\n         ↓\nMermaid renderizado en frontend\n         ↓\n[Click \"Generar PDF\"]\n         ↓\ntrpc.pdf.generateCertificate\n         ↓\nHTML generado\n         ↓\nDescarga o impresión\n```\n\n## Testing\n\n### Casos de Prueba Recomendados\n\n1. **Cálculos Eléctricos**\n   - Vivienda unifamiliar 9200W\n   - Local comercial 15000W\n   - Nave industrial 50000W\n\n2. **Sugerencias IA**\n   - Verificar circuitos sugeridos por tipo\n   - Validar protecciones recomendadas\n\n3. **Generación de Diagrama**\n   - Verificar código Mermaid válido\n   - Comprobar renderizado correcto\n\n4. **Generación de PDF**\n   - Verificar HTML válido\n   - Comprobar todos los datos incluidos\n   - Validar formato profesional\n\n## Notas de Implementación\n\n### Limitaciones Actuales\n\n1. **PDF**: Actualmente genera HTML. Para PDF real se necesita:\n   - `html2pdf` (librería Node.js)\n   - `puppeteer` (headless browser)\n   - `wkhtmltopdf` (CLI tool)\n\n2. **IA**: Las sugerencias son determinísticas basadas en reglas. Para IA real:\n   - Integrar OpenAI API\n   - Implementar rate limiting\n   - Agregar caché de sugerencias\n\n3. **Diagramas**: Usa Mermaid. Alternativas:\n   - GraphViz\n   - Draw.io\n   - Custom SVG\n\n### Mejoras Futuras\n\n1. **Fase 2**\n   - Integración real con OpenAI GPT-4\n   - Conversión HTML a PDF con Puppeteer\n   - OCR de fotos de instalaciones\n   - Exportar a Excel/Word\n\n2. **Fase 3**\n   - Integración API distribuidoras\n   - Registro automático en Junta\n   - Biblioteca de plantillas empresa\n   - App móvil nativa\n\n## Soporte\n\nPara preguntas o problemas:\n1. Revisar el PRD completo en `/home/ubuntu/upload/pasted_content.txt`\n2. Consultar el schema de BD en `drizzle/schema.ts`\n3. Revisar los tipos en `shared/types.ts`\n\n## Checklist Final\n\n- [ ] Integrar Step4Enhanced en CertificateWizard\n- [ ] Integrar Step6Enhanced en CertificateWizard\n- [ ] Probar flujo completo del wizard\n- [ ] Validar cálculos eléctricos\n- [ ] Verificar generación de diagramas\n- [ ] Verificar generación de certificados\n- [ ] Agregar tests unitarios\n- [ ] Documentar endpoints tRPC\n- [ ] Realizar testing e2e\n- [ ] Optimizar performance\n
